[project]
name = "scrna-all"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anndata>=0.12.6",
    "jupyter>=1.1.1",
    "scanpy>=1.11.5",
    "zarrs>=0.2.2",
]
